# ATBCmder Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and deploy a Zensical-based official website for ATBCmder, featuring a custom landing page and the existing documentation.

**Architecture:** A static site generated by Zensical using the Material theme, with a custom Jinja2 template overriding the `home.html` layout for the landing page. Deployed to GitHub Pages via GitHub Actions.

**Tech Stack:** Zensical, Material for MkDocs, HTML, CSS, GitHub Actions.

## Global Constraints

- No existing Markdown files in `docs/` should be moved, renamed, or modified (except creating `docs/index.md`).
- Primary language is English; multilingual support is explicitly out of scope.
- Use `zensical` commands (`zensical serve`, `zensical build`), not `mkdocs`.
- GitHub Pages source must be the `gh-pages` branch.

---

### Task 1: Site Configuration and Base Setup

**Files:**
- Create: `zensical.toml`
- Create: `docs/index.md`

**Interfaces:**
- Consumes: Existing docs in `docs/`
- Produces: Base Zensical site configuration capable of running `zensical serve`

- [ ] **Step 1: Write the Zensical configuration**

```toml
[project]
site_name    = "ATBCmder"
site_url     = "https://aitobox.github.io/ATBCmderDoc/"
site_description = "The Dual-Panel File Manager for macOS"
repo_url     = "https://github.com/aitobox/ATBCmderDoc"
repo_name    = "ATBCmderDoc"

[theme]
name        = "material"
custom_dir  = "overrides"
palette     = [
  { scheme = "default", primary = "indigo", accent = "deep-purple", media = "(prefers-color-scheme: light)" },
  { scheme = "slate",   primary = "indigo", accent = "deep-purple", media = "(prefers-color-scheme: dark)" }
]
font        = { text = "Roboto", code = "Roboto Mono" }
features    = [
  "navigation.tabs",
  "navigation.top",
  "search.highlight",
  "content.code.copy"
]

[extra]
app_store_url = "https://apps.apple.com/app/atbcmder/idXXXXXXXX"

[extra_css]
files = ["stylesheets/extra.css"]
```

- [ ] **Step 2: Create the placeholder index.md**

```markdown
---
template: home.html
---
```
*(This triggers the custom home template we will build in Task 2. The existing `index.md` will be overwritten)*

- [ ] **Step 3: Verify configuration**

Run: `zensical build`
Expected: Builds successfully, though `home.html` is not yet defined so the homepage might fall back or be blank.

- [ ] **Step 4: Commit**

```bash
git add zensical.toml docs/index.md
git commit -m "chore: setup Zensical configuration and index placeholder"
```

---

### Task 2: Landing Page Template (HTML)

**Files:**
- Create: `overrides/home.html`

**Interfaces:**
- Consumes: Material theme base template, `extra.app_store_url` from config
- Produces: The HTML structure for the landing page

- [ ] **Step 1: Create the template structure**

```html
{% extends "base.html" %}

{% block content %}
<div class="atb-home">
  <!-- Hero Section -->
  <section class="atb-hero">
    <div class="atb-hero-content">
      <h1>ATBCmder</h1>
      <p class="atb-tagline">The Dual-Panel File Manager for macOS</p>
      <p class="atb-subtitle">Navigate, manage, and transform your files with the speed and power of a professional tool.</p>
      <div class="atb-cta-container">
        <a href="{{ config.extra.app_store_url }}" class="atb-app-store-btn" target="_blank" rel="noopener">
          Download on the App Store
        </a>
        <span class="atb-coming-soon">Coming soon &middot; Stay tuned</span>
      </div>
    </div>
  </section>

  <!-- Features Grid -->
  <section class="atb-features">
    <div class="atb-container">
      <div class="atb-grid">
        <div class="atb-card">
          <span class="atb-icon">🗂</span>
          <h3>Dual Panel</h3>
          <p>View and manage two directories side-by-side simultaneously</p>
        </div>
        <div class="atb-card">
          <span class="atb-icon">🌐</span>
          <h3>Network VFS</h3>
          <p>FTP, SFTP, WebDAV & Samba — remote files feel local</p>
        </div>
        <div class="atb-card">
          <span class="atb-icon">📦</span>
          <h3>Archive VFS</h3>
          <p>Browse and edit ZIP, TAR, 7z without extracting</p>
        </div>
        <div class="atb-card">
          <span class="atb-icon">✨</span>
          <h3>Semantic Command</h3>
          <p>Natural language file search powered by macOS Spotlight</p>
        </div>
        <div class="atb-card">
          <span class="atb-icon">🌿</span>
          <h3>Branch View</h3>
          <p>See all nested files in a single flat list instantly</p>
        </div>
        <div class="atb-card">
          <span class="atb-icon">🌍</span>
          <h3>30+ Languages</h3>
          <p>Automatically follows your system language</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Screenshots -->
  <section class="atb-screenshots">
    <div class="atb-container">
      <div class="atb-screenshots-grid">
        <figure class="atb-screenshot-figure">
          <img src="images/semantic_command.png" alt="Semantic Command">
          <figcaption>Natural language file search</figcaption>
        </figure>
        <figure class="atb-screenshot-figure">
          <img src="images/branch_view.png" alt="Branch View">
          <figcaption>Flat view of nested directories</figcaption>
        </figure>
        <figure class="atb-screenshot-figure">
          <img src="images/middle_toolbar.png" alt="Middle Toolbar">
          <figcaption>Quick access to essential commands</figcaption>
        </figure>
      </div>
    </div>
  </section>

  <!-- Bottom CTA -->
  <section class="atb-bottom-cta">
    <div class="atb-container">
      <h2>Ready to explore ATBCmder?</h2>
      <a href="01_getting_started/" class="atb-primary-btn">Read the Documentation &rarr;</a>
    </div>
  </section>
</div>
{% endblock %}
```

- [ ] **Step 2: Verify HTML generation**

Run: `zensical build`
Expected: Builds successfully, HTML structure should be present in `site/index.html`.

- [ ] **Step 3: Commit**

```bash
git add overrides/home.html
git commit -m "feat: add landing page HTML template"
```

---

### Task 3: Landing Page Styling (CSS)

**Files:**
- Create: `docs/stylesheets/extra.css`

**Interfaces:**
- Consumes: HTML classes defined in Task 2
- Produces: Visual styling for the landing page

- [ ] **Step 1: Write the CSS**

```css
/* Base variables mapped to Material theme colors */
:root {
  --atb-primary: var(--md-primary-fg-color);
  --atb-accent: var(--md-accent-fg-color);
}

.atb-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
}

/* Hero Section */
.atb-hero {
  background: linear-gradient(135deg, var(--atb-primary) 0%, var(--atb-accent) 100%);
  color: white;
  padding: 4rem 1rem;
  text-align: center;
  border-radius: 0 0 2rem 2rem;
  margin-bottom: 3rem;
}

.atb-hero-content {
  max-width: 800px;
  margin: 0 auto;
}

.atb-hero h1 {
  font-size: 3.5rem;
  font-weight: 700;
  margin: 0 0 1rem;
  color: white;
}

.atb-tagline {
  font-size: 1.5rem;
  font-weight: 500;
  margin-bottom: 1rem;
}

.atb-subtitle {
  font-size: 1.2rem;
  opacity: 0.9;
  margin-bottom: 2rem;
}

.atb-cta-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
}

.atb-app-store-btn {
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.3);
  color: white;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  text-decoration: none;
  font-weight: 500;
  backdrop-filter: blur(10px);
  transition: background 0.2s;
}

.atb-app-store-btn:hover {
  background: rgba(255, 255, 255, 0.2);
  color: white;
}

.atb-coming-soon {
  font-size: 0.875rem;
  opacity: 0.7;
}

/* Features Grid */
.atb-features {
  margin-bottom: 4rem;
}

.atb-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.atb-card {
  background: var(--md-default-bg-color);
  border: 1px solid var(--md-default-fg-color--lightest);
  border-radius: 1rem;
  padding: 1.5rem;
  transition: transform 0.2s, box-shadow 0.2s;
}

.atb-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(0,0,0,0.05);
}

.atb-icon {
  font-size: 2rem;
  margin-bottom: 1rem;
  display: block;
}

.atb-card h3 {
  margin: 0 0 0.5rem;
  font-size: 1.25rem;
}

.atb-card p {
  margin: 0;
  color: var(--md-default-fg-color--light);
}

/* Screenshots */
.atb-screenshots {
  margin-bottom: 4rem;
}

.atb-screenshots-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.atb-screenshot-figure {
  margin: 0;
  background: var(--md-default-bg-color);
  border: 1px solid var(--md-default-fg-color--lightest);
  border-radius: 0.5rem;
  overflow: hidden;
  box-shadow: 0 4px 6px rgba(0,0,0,0.05);
}

.atb-screenshot-figure img {
  width: 100%;
  height: auto;
  display: block;
  border-bottom: 1px solid var(--md-default-fg-color--lightest);
}

.atb-screenshot-figure figcaption {
  padding: 1rem;
  text-align: center;
  font-size: 0.875rem;
  color: var(--md-default-fg-color--light);
}

/* Bottom CTA */
.atb-bottom-cta {
  text-align: center;
  padding: 4rem 1rem;
  background: var(--md-default-bg-color);
  border-top: 1px solid var(--md-default-fg-color--lightest);
}

.atb-bottom-cta h2 {
  margin: 0 0 1.5rem;
}

.atb-primary-btn {
  display: inline-block;
  background: var(--atb-primary);
  color: var(--md-primary-bg-color);
  padding: 0.75rem 2rem;
  border-radius: 2rem;
  text-decoration: none;
  font-weight: 500;
  transition: filter 0.2s;
}

.atb-primary-btn:hover {
  filter: brightness(110%);
  color: var(--md-primary-bg-color);
}
```

- [ ] **Step 2: Verify CSS application**

Run: `zensical build`
Expected: Builds successfully. CSS file is copied to `site/stylesheets/extra.css`.

- [ ] **Step 3: Commit**

```bash
git add docs/stylesheets/extra.css
git commit -m "feat: add styling for landing page"
```

---

### Task 4: GitHub Actions Deployment Setup

**Files:**
- Create: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: Source code in `main` branch
- Produces: Built site published to `gh-pages` branch

- [ ] **Step 1: Write the workflow file**

```yaml
name: Deploy Zensical Docs
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.x
          
      - name: Install dependencies
        run: |
          pip install zensical
          
      - name: Build site
        run: zensical build
        
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          publish_branch: gh-pages
```

- [ ] **Step 2: Commit**

```bash
mkdir -p .github/workflows
git add .github/workflows/deploy.yml
git commit -m "ci: add GitHub Actions workflow for Zensical deployment"
```
