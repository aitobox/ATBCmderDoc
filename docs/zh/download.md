# 下载与安装 ATBCmder

感谢您对 ATBCmder 的关注与支持！我们提供两种便捷的安装方式，满足不同的使用偏好与场景需求。

> [!IMPORTANT]
> **系统与硬件芯片要求**  
> - **支持系统**：macOS 12.0 (Monterey) 及更高版本（全面兼容 macOS 13 Ventura、macOS 14 Sonoma 与 macOS 15 Sequoia）。  
> - **硬件架构支持**：仅支持 **Apple Silicon (M1 / M2 / M3 / M4，ARM64 架构)** 芯片。  
> - **Intel (x86_64) 架构说明**：目前应用**暂不支持** Intel (x86_64) 架构的 Mac 电脑。

## 1. Mac App Store 商店版（推荐）

这是最推荐普通用户选择的安装方式。**ATBCmder 现已正式上架 Mac App Store！** 通过官方商店下载，可享受全自动后台静默更新、原生 macOS 应用沙盒安全保护以及最深度的系统级整合。

- **Mac App Store 官方下载**：[在 Mac App Store 中下载 ATBCmder](https://apps.apple.com/app/atbcmder/id6792398333)

## 2. 独立 DMG 安装包下载

若您无法访问 Mac App Store，或偏好直接使用独立分发版本，我们同时提供专为 Apple Silicon (ARM64) 芯片原生构建的 DMG 磁盘映像安装包。

- **DMG 安装包直接下载**：[点击下载最新版 ATBCmder DMG 镜像](https://storage.aitobox.com/@s/bNAshV6G/ATBCmder)（仅限 Apple Silicon / ARM64 架构）

*注意：通过 DMG 首次打开应用时，macOS 安全机制可能会提示“无法打开或未验证的应用”。只需前往 **系统设置 > 隐私与安全性**，点击“仍要打开”即可正常运行。目前暂不支持 Intel (x86_64) 架构设备。*

### ⚠️ 授予“完全磁盘访问权限”

ATBCmder 作为专业级双面板文件管理器，需要访问并管理您的全盘文件。为保证全部功能顺畅运行，请按照以下步骤授予权限：

1. 点击屏幕左上角 Apple 图标（），打开 **系统设置**（较早的 macOS 版本中为“系统偏好设置”）。
2. 在左侧或主菜单中找到并点击 **隐私与安全性**。
3. 向下滚动列表，点击进入 **完全磁盘访问权限**（Full Disk Access）。
4. 在应用列表中找到 **ATBCmder** 并打开右侧开关。若列表中未显示，可点击下方的 **+** 号按钮手动添加 。
5. 系统将提示您输入 Mac 登录密码或轻触 Touch ID 确认更改。

---

## 3. 版本更新日志 (Release Notes)

### 1.7.0 (2026-09-06)

- 全能文档与媒体预览器：新增 Office 预览套件（Excel 表格虚拟懒加载、Word 文档分页与图片渲染、PowerPoint 幻灯片卡片视图）；新增 SQLite、Markdown 大纲、Jupyter Notebook、字体、压缩包、音频等 8+ 种格式预览；支持 GB 级超大文本文件秒级流式无限滚动
- 双向文件差异比较器：全新内置侧边双向 Diff 工具，支持逐行/行内字符高亮、差异块一键合并、撤销编辑与编码无损保存
- 搜索系统全面重构：默认支持模糊子串与正则搜索；新增“导入到列表面板 (Feed to ListBox)”标签页功能；优化海量结果批量流式渲染，彻底消除 UI 卡顿
- 文件操作与核心传输加固：解决跨设备移动重复弹窗确认与传输队列死锁问题；属性对话框新增文件创建时间；默认启用居中垂直工具栏并优化活动面板视觉指示

### 1.6.2 (2026-09-03)

- 鼠标框选与选择交互增强：列表视图与缩略图视图全新支持鼠标拖拽框选多选（Rubberband Drag Selection），并支持点击空白区域快速取消选择
- 全局窗口快捷键与多键录制：新增可自定义的全局窗口呼出/隐藏快捷键；修复快捷键设置中无法正确录制 4 键组合快捷键的问题
- macOS 托盘与 Dock 恢复修复：修复应用隐藏至系统托盘后从 Dock 图标点击无法正常还原主窗口或显示空白窗口的问题
- 稳定性加固与核心健壮性提升：修复多线程与对象生命周期中的潜在崩溃，加固 VFS 虚拟文件系统并全面优化测试稳定性

### 1.6.1 (2026-08-27)

- macOS 原生 UI/UX 全面重构：深度遵循 Apple HIG 设计规范，优化深浅色主题调色板、Finder 风格面板高亮、卡片式 ToolTip 与平滑过渡动画
- 矢量图标引擎与现代控件：内置 SF Symbols 风格矢量图标生成器，重构驱动器按钮、面包屑导航与下拉控件，全面支持高分屏自适应
- 交互与布局升级：多重重命名对话框重构为双栏布局并新增重名实时冲突检测；缩略图视图支持 `Cmd + 滚轮` 动态无级缩放；新增统一的空状态提示组件

### 1.6.0 (2026-08-27)

- 双分发渠道流水线：支持面向独立官网分发 (DMG) 与 Mac App Store 商店版的双轨自动化构建体系
- 商店版合规与菜单适配：在 App Store 版本中自动隐藏外置更新检查入口，确保严格符合 Apple 审核规范；独立分发版继续保留手动更新入口
- 二进制合规补丁：内置 `itms-services` 专用二进制扫描与安全修复工具，彻底解决 Qt/PySide6 构建产物通过 App Store Connect 自动化验证时的私有协议拦截问题

### 1.5.6 (2026-08-22)

- 标签页独立视图模式：每个标签页支持独立的视图配置（平铺模式 Flat View / 树形模式 Tree Mode），切换标签页自动同步显示状态并在会话中完整持久化
- 智能文件打开与系统回退：新增多层级文件类型检测器（魔数/MIME/扩展名），内置支持的文档与媒体无缝调用内置查看器，未支持的文件格式自动回退至系统默认程序打开

### 1.5.5 (2026-08-21)

- 应用更新检查策略优化：默认禁用启动时的自动联网检查更新，改为纯手动触发检查（通过菜单栏 `帮助` -> `检查更新...`），提升软件启动速度并优化离线使用体验

### 1.5.4 (2026-08-21)

- 焦点高亮与选择状态优化：彻底修复双面板切换时非活动面板残留焦点框的问题；优化跨面板移动文件后的选择项重置逻辑，防止残留选区导致误操作

### 1.5.3 (2026-08-19)

- 面包屑级联子菜单交互优化：精确对齐多级子目录菜单展开时的高亮行垂直高度，消除多层级穿透时的位置偏移与跳动，极大提升快捷目录浏览体验

### 1.5.2 (2026-08-19)

- 启动与版本说明修复：修复每次启动应用时重复弹出“版本说明”对话框的问题；增强配置系统 `Config.get` 默认值处理，确保仅在首次运行或版本升级后展示新特性提示

### 1.5.1 (2026-08-19)

- 多语言版本说明支持：新增 7 种主流语言 ReleaseNotes（繁中/日/韩/德/法/俄/西），实现多级智能回退解析与动态自动发现
- 打包签名机制优化：重构 macOS 签名脚本与时间戳重试策略，优化 Mach-O 二进制文件签名流程，彻底解决构建限流与签名失败问题

### 1.5.0 (2026-08-17)

- 全局会话持久化 (Session Manager)：实现应用级别的会话状态保存与恢复机制，无缝恢复上次关闭时的所有左右面板标签页、目录路径、展开模式 (Flat/Tree View)、面板比例及多显示器窗口位置
- 配置导入与导出：新增全局配置与数据 `一键导出/导入 ZIP` 功能，方便设备间数据迁移
- 图片编辑器增强：新增自定义图像尺寸调整 (Resize) 功能，支持按比例缩放锁定及预设分辨率快速缩放

### 1.4.9 (2026-08-17)

- 全新内置图片编辑器：支持图像裁剪缩放、精细旋转、多级滤镜调整，并引入基于 OpenCV 的 AI 去水印及图像修复 (Inpainting) 功能
- 高级图片查看器增强：全面支持动态 GIF 播放，新增图像平移、缩放、旋转、EXIF 属性查看以及幻灯片自动播放功能
- 搜索集成优化：深度集成 macOS 原生 Spotlight 搜索引擎，实现文件秒搜并优化了搜索结果的焦点定位

### 1.4.8 (2026-08-16)

- 国际化 (i18n) 全面深化：完成界面、F3 查看器与 F4 编辑器的多语言全面审计与补充翻译，极大改善了音视频播放器及各类预览控件的本地化体验
- 快速查看器 (F3) 优化：完善轻量级 EPUB 阅读器 (SimpleEpubPanel) 的外链跳转与循环搜索逻辑；精简 PDF 阅读器工具栏，移除冗余的旋转按钮

### 1.4.7 (2026-08-16)

- 音视频播放器增强：大幅度优化内置媒体播放器的交互体验与稳定性
- 编译系统重构：重命名 `BUILD_EPUB` 环境变量为 `BUILD_WEBENGINE`，解耦轻量级打包逻辑
- 安装包依赖修复：确保轻量版 DMG 安装包也始终内置 `ebooklib`，保障后备电子书阅读器 (SimpleEpubPanel) 的可用性

### 1.4.6 (2026-08-16)

- 内置轻量级 EPUB 阅读器 (SimpleEpubPanel)：新增无需依赖 WebEngine 的后备电子书阅读引擎，支持章节跳转与搜索，并提供 `--epub-reader` 命令行参数强制指定阅读引擎
- 面包屑菜单交互重构 (BreadcrumbFolderMenu)：完全重构面包屑下拉菜单为单列滚动样式，并支持无限级联子菜单直达，彻底解决多级菜单展示重叠与鼠标焦点锁定问题

### 1.4.5 (2026-08-15)

- 全新代码查看器 (F3)：支持百种语言语法高亮、大文件动态跟随 (Tail)、正则表达式搜索及行号显示
- 全新代码编辑器 (F4)：内置编码/换行符动态转换、安全原子写入、缩进调整与查找替换功能
- 全局外部拖拽 (Drag & Drop)：支持将文件或压缩包内的深层文件，直接无缝拖拽至桌面或第三方应用 (例如 VSCode / Sublime)

### 1.4.4 (2026-08-15)

- 界面可读性增强：全面放大菜单栏、按钮、提示框与设置界面的字体字号，提升视觉舒适度
- 游标交互修复：修复调整窗口边缘、面板分割线及表格列宽时鼠标调整指针(Cursor)丢失的问题
- Mac 原生控件修复：解决部分组件(QComboBox下拉框、QGroupBox标题)在 macOS 下背景发黑或溢出的显示异常

### 1.4.3 (2026-08-14)

- 全新 macOS 原生交互风格 (Aqua Blue Theme)：新增专属主题并设为默认，引入交互式面包屑导航 (支持级联子目录直达)、Mac 风格存储容量条 (修复大容量溢出显示) 及圆角胶囊选择高亮
- 界面细节深度打磨：重构标签页关闭按钮布局，修复命令行输入框重叠问题，并为所有主题(明/暗/经典)统一下巴栏背景、分割线与面板边框
- 国际化与升级支持：完成全项目 i18n 多语言翻译覆盖审计；自动更新模块新增对 AList/OpenList 聚合网盘分享协议的支持

### 1.4.2 (2026-08-13)

- 智能语义过滤器增强：支持更多常见文件扩展名解析与单复数匹配，并全面支持多语言 (i18n)
- 语义搜索体验优化：采用真实的进度条替换等待光标，优化按回车键触发补全与执行的交互逻辑
- 核心语义解析优化：修复目标类型推断与关键词过滤逻辑，提升自然语言搜索准确性

### 1.4.1 (2026-08-13)

- 全局代码质量审查与深度重构 (批次 A-D)
- 安全增强：修复可能存在的路径遍历等安全隐患
- 并发与性能优化：改善后台任务线程安全与执行效率
- 架构优化：提升核心组件的稳定性与资源管理

### 1.4.0 (2026-08-12)

- 深度修复 VFS 网络模块：包括多编码支持、协议驱动修复、链式压缩包挂载支持与传输冲突修复
- 完善应用自动升级模块：修复证书验证、安装包验证与界面集成问题
- 版本更新提示优化：自动更新时展示完整的版本历史日志

### 1.3.9 (2026-08-12)

- 新增内置 EPUB 电子书阅读器 (F3 快速预览)
- 新增应用自动升级检测与提示机制
- 修复 VFS 文件冲突处理时的进程卡死问题

### 1.3.8 (2026-08-08)

- 树状视图 (Tree View) UI 优化
- 深度重构基于 fsspec 的网络集成
- 优化非法文件字符的处理逻辑

### 1.3.7 (2026-08-06)

- 网络 VFS 引擎重构：基于 `fsspec` 深度重构网络文件系统（FTP/WebDAV/SMB），实现异步目录加载 `VfsTableModel`、流式传输 `StreamCopyWorker`、远程新建/重命名/删除/覆盖冲突提示，以及 F3/F4 远程预览与编辑
- 系统托盘与全局热键：支持最小化到系统托盘、全局快捷键 (`Option+Cmd+H`) 切换托盘、macOS Dock 图标动态显隐与原生模板图标适配
- LLM 配置与 AI 语义过滤：新增 `default_llm.xml` 配置单例与结构控制，集成 AI 语义过滤器及选项设置页面
- 快捷键与导航增强：常用列表弹窗及文件面板全面支持 PageUp / PageDown / Home / End / Fn 导航快捷键
- 国际化 i18n 补充：全面封装 VFS 错误提示、快速查看面板与托盘菜单的多语言翻译与 Catalog 编译

### 1.3.6 (2026-08-04)

- 网络 VFS 增强：优化 FTP 编码自动检测 (UTF-8/GBK/Makefile 动态更新)、传输降级与 Socket 重置；解决 FTP 连接超时及响应错位问题
- WebDAV 与 SMB 修复：修复 WebDAV/SMB 根路径剥离、子目录异常日志传播、连接异常及虚拟文件系统图标/标签标题显示
- 缩略图视图导航：实现缩略图网格视图 (Thumbnail View) 2D 键盘方向键平滑导航
- 国际化与代码质量：修复 zh_CN/zh_TW 语言包中“关闭标签页”翻译损坏问题，完成全代码库审查优化与安全硬化

### 1.3.5 (2026-08-03)

- 快速查看面板：实现快速查看功能 (`cm_QuickView`, `Cmd+Q`/`Ctrl+Q`)，支持多格式预览、文件属性兜底展示、左右对称翻转、显示菜单集成与完整 i18n 多语言
- 快速查看组件：新增 `QuickViewContainer` 与 `QuickViewPropertiesWidget` 容器集成至文件面板
- 界面细节修复：修复浅色主题下标签页头部对齐及选项设置对话框页面挤压的问题

### 1.3.4 (2026-08-02)

- 文件面板界面：新增 Shift+PageUp / Shift+PageDown 批量选择文件功能
- 文件操作修复：修复 F5 复制与 F6 移动文件/目录的相关缺陷
- 构建与打包脚本：修复多张证书匹配歧义错误，给签名加上时间戳并优雅处理 macOS 公证状态

### 1.3.3 (2026-08-01)

- 核心传输引擎：实现 ProcessTransferWorker 实时传输速度与剩余时间 (ETA) 准确计算
- 权限与沙盒机制：分离 macOS 沙盒机制与完全磁盘访问权限 (Full Disk Access) 逻辑流程
- 构建打包脚本：更新打包脚本配置，支持 1.3.3 DMG 签名构建

### 1.3.2 (2026-08-01)

- 修复 Nuitka 独立打包与 App Store 模式下传输子进程冻结/无法启动的问题
- 代码审查 Skill：增加 i18n 多语言完整性审计检查规范

### 1.3.1 (2026-08-01)

- 修复 macOS 沙盒下因 `os.access` 校验导致误判“完全磁盘访问权限已授予”的问题
- 修复并发后台任务状态交错干扰问题
- 补充无回收站删除确认对话框中批量操作复选框的中文翻译
- 增加与更新代码审查优化审计 Agent Skill

### 1.3.0 (2026-08-01)

- 核心传输引擎：实现跨进程隔离的文件传输引擎 (ProcessTransferWorker & ProcessIOEngine)，彻底解决大文件/Samba 传输阻塞 UI 主线程问题
- 传输性能优化：加入 10Hz IPC 频率限速控制与 macOS `F_NOCACHE` 文件缓存优化，显著降低 GUI 卡顿与 CPU/内存占用
- 架构与并发重构：完成 4 阶段代码审查修复、安全硬化、并发互斥锁与底层引擎重构
- 界面与细节修复：修复 Shiboken C++ 对象销毁异常、水平双面板布局切换与后台进度指示器信号断开问题

### 1.2.0 (2026-07-30)

- 实现全局 I/O 进程池 (IoWorkerPool)，隔离阻塞式文件系统 I/O 防止 GUI 卡顿
- 配置文件版本控制：支持 XML 根节点读写 app_version，增加配置自动迁移 Runner 机制
- 拖拽与剪贴板：集成 macOS Finder 原生桥接，支持文件拖放及剪贴板状态机
- 路径展开：新增 `expand_path` 工具，支持 `~`、`$VAR`、`%VAR%` 及 `%COMMANDER_PATH%`
- 常用列表：实现独立 HotlistConfig 单例配置管理及内置默认常用列表

### 1.1.0 (2026-07-29)

- 修正发布日志添加顺序，确保按最新时间倒序排列在 Release Note 标题下方
- 修正版本管理脚本，增加对 default_config.xml 中 System/LastVersion 节点的同步更新
- 快捷键：为标签切换添加 Meta+Tab 与 Meta+Shift+Tab 默认快捷键
- 收藏标签：增加自动迁移与清理主配置中的旧收藏标签数据
- 文件查看器：优化大文件加载性能与内存占用

### 1.0.1 (2026-07-22)

> 修正了沙盒环境下，无法正常预览文本文件的问题

### 1.0.0 (2026-07-18)

> TotalCommander MacOS移植版的初始实现。
