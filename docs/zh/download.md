# 下载 ATBCmder

感谢您对 ATBCmder 的兴趣！我们为您提供了两种不同的下载与安装方式，以满足您的需求。

## 1. Mac App Store (推荐)

这是最推荐的安装方式。通过苹果官方应用商店下载，您可以获得自动更新和最完整的系统集成。

> **🚧 正在申请上架中**  
> 我们的应用目前正在 Mac App Store 的审核上架过程中，敬请期待！在此期间，请使用下方的 DMG 方式进行安装。

[Mac App Store 下载页面](https://apps.apple.com/app/atbcmder/6792398333)

## 2. DMG 安装包下载

如果您由于网络原因无法访问 Mac App Store，或者偏好使用直接下载的方式，我们提供了 DMG 安装包。

- **DMG 安装包下载链接**: [点击下载 ATBCmder DMG 文件](https://storage.aitobox.com/@s/bNAshV6G/ATBCmder)

*注意：通过 DMG 方式安装后，由于 macOS 的安全机制，首次打开时可能需要在“系统设置 > 隐私与安全性”中允许打开该应用。*

### ⚠️ 赋予完全磁盘访问权限

ATBCmder 是一个文件管理工具，需要用户给予磁盘管理的权限，操作如下：

1. 点击屏幕左上角的苹果图标，选择 **系统设置**（旧版本为“系统偏好设置”）。
2. 在左侧或右侧菜单中找到并点击 **隐私与安全性**。
3. 向下滚动并点击 **完全磁盘访问权限**。
4. 在列表中找到您需要授权的应用 (**ATBCmder**)，并打开右侧的开关。如果应用不在列表中，可以点击底部的 **+** 号手动添加。
5. 系统会要求你输入 Mac 的开机密码或使用 Touch ID 来确认修改。

## 更新日志 (Release Notes)

### 1.4.2 (2026-08-13)

- **AI 语义过滤器增强**：支持解析更多常见扩展名、复数形式，并完整集成国际化 (i18n)。
- **语义搜索 UX 优化**：将等待光标替换为 QProgressBar，并改进 Enter 键的补全交互体验。
- **核心语义解析**：优化目标类型检测与关键字剥离逻辑，实现更精准的自然语言搜索。

### 1.4.1 (2026-08-13)

- **全量代码库审查与重构**（批次 A-D）。
- **安全加固**：修复潜在的路径穿越和访问权限漏洞。
- **并发与性能**：提升后台线程安全性与执行效率。
- **架构优化**：增强核心组件的稳定性与资源管理能力。

### 1.4.0 (2026-08-12)

- **深度 VFS 网络修复**：多编码支持、协议驱动程序、链式压缩包挂载与冲突解决。
- **应用升级模块增强**：修复 SSL 校验、DMG 下载校验以及 UI 集成。
- **更新日志界面**：自动更新过程中可显示完整的历史版本日志。

### 1.3.9 (2026-08-12)

- **内置 EPUB 阅读器**：增加 EPUB 图书快捷预览 (F3 Quick View)。
- **应用升级检测**：实现软件版本更新检测机制。
- **VFS 优化**：修复解决文件冲突时 VFS Worker 的冻结问题。

### 1.3.8 (2026-08-08)

- **树状视图 UI 细节优化**。
- **重构 fsspec 网络集成**。
- **改进非法文件字符的处理**。

### 1.3.7 (2026-08-06)

- **网络 VFS 架构**：使用 `fsspec` 重构网络文件系统 (FTP/WebDAV/SMB)，实现异步 `VfsTableModel`、流式 `StreamCopyWorker`、远程文件操作（新建目录/重命名/删除/覆盖）以及 F3/F4 远程查看与编辑。
- **系统托盘与快捷键**：支持最小化至系统托盘、全局快捷键 (`Option+Cmd+H`)、macOS Dock 图标切换以及原生模板图标集成。
- **LLM 配置**：引入 `default_llm.xml` 资源及单例，用于 AI 语义过滤与选项设置 UI。
- **导航增强**：文件面板与常用目录弹窗全面支持 PageUp / PageDown / Home / End / Fn 快捷导航键。
- **国际化 (i18n)**：对所有 VFS 错误信息、快速预览面板标签和系统托盘菜单文本包装国际化翻译支持。

### 1.3.6 (2026-08-04)

- **网络 VFS 增强**：增加对 FTP 的 UTF-8/GBK 自动检测、动态更新编码、回退处理以及 Socket 重置功能；解决超时与应答不同步的问题。
- **WebDAV 与 SMB 修复**：修复 WebDAV/SMB 根路径截断问题、传播 last_error 错误、增强连接稳定性，修复 VFS 图标和标签页标题显示。
- **缩略图导航**：在缩略图视图中实现流畅的 2D 网格方向键导航功能。
- **国际化与代码质量**：修复简体/繁体中文包中损坏的“关闭标签页”翻译问题，完成整个代码库的优化审计。

### 1.3.5 (2026-08-03)

- **快速预览面板**：实现快速预览 (Quick View) 功能 (`cm_QuickView`, `Cmd+Q`/`Ctrl+Q`)，支持多格式预览、后备文件属性显示、左右对称切换，并整合进入 Show 菜单，支持完整国际化。
- **快速预览组件**：新增 `QuickViewContainer` 与 `QuickViewPropertiesWidget` 并将其集成至文件面板 (FilePanel) 中。
- **UI 优化**：修复明亮主题下标签页头部对齐问题，以及选项设置对话框中页面被挤压的问题。

### 1.3.4 (2026-08-02)

- **文件面板 UI**：在文件面板视图中增加 Shift+PageUp / Shift+PageDown 的批量选择支持。
- **文件操作**：修复 F5 复制和 F6 移动文件/文件夹的相关缺陷。
- **构建脚本**：精确设置证书标识名称，增加代码签名的 timestamping，并优雅处理公证状态无效的问题。

### 1.3.3 (2026-08-01)

- **传输引擎**：在 ProcessTransferWorker 中精确计算实时传输速度和剩余时间 (ETA)。
- **权限与沙盒**：将 macOS 沙盒检查与“完全磁盘访问权限”检测流程分离。
- **构建脚本**：更新针对 1.3.3 签名版 DMG 构建脚本的配置。

### 1.3.2 (2026-08-01)

- **子进程传输后台**：修复在 Nuitka 独立 App Store 捆绑模式下，进程冻结及启动失败的问题。
- **Agent 技能**：在代码审核优化技能中，增加国际化 (i18n) 完整性审计检查。

### 1.3.1 (2026-08-01)

- **macOS 沙盒**：修复因 `os.access` 检查导致错误判定“完全磁盘访问已授权”状态的问题。
- **并发任务**：解决并发后台操作导致的状态串扰问题。
- **国际化 (i18n)**：增加永久删除对话框中批量操作复选框的中文翻译。
- **Agent 技能**：新增并更新代码审查与优化审计技能。

### 1.3.0 (2026-08-01)

- **进程隔离传输引擎**：实现 ProcessTransferWorker 与 ProcessIOEngine，将文件复制/移动的 I/O 操作从 UI 主线程中剥离。
- **传输性能与响应**：应用 10Hz IPC 速率限制、自适应缓冲以及 macOS 的 `F_NOCACHE` 优化，以防止 GUI 界面卡顿。
- **代码审计与加固**：完成 4 阶段的重构，包含并发互斥锁、安全加固及架构清理。
- **UI 修复**：修复 Shiboken C++ 对象被删除导致的报错、水平面板模式的布局问题，以及后台任务进度指示器的信号问题。

### 1.2.0 (2026-07-30)

- 实现全局 I/O 进程池 (IoWorkerPool)，以隔离阻塞式的文件 I/O 操作并防止 GUI 卡死。
- **配置版本管理**：在 XML 根节点读/写 `app_version`，并添加自动化数据迁移运行器注册表。
- **拖拽与剪贴板**：集成 macOS 访达 (Finder) 原生桥接机制、自动展开式文件夹支持 (spring-loaded folders)，以及剪贴板状态机。
- **路径展开**：新增共享 `expand_path` 工具，支持解析 `~`、`$VAR`、`%VAR%` 和 `%COMMANDER_PATH%`。
- **常用目录列表**：实现独立的 HotlistConfig 单例并配置默认热键目录。

### 1.1.0 (2026-07-29)

- 修复发布日志 (Release Note) 排序，确保在标题下按时间倒序排列。
- 修复版本管理器脚本，使其支持 `default_config.xml` 中的 System/LastVersion XML 节点。
- **快捷键**：增加 Meta+Tab 与 Meta+Shift+Tab 快捷键用于标签页导航。
- **收藏标签组**：新增从主配置中对遗留收藏标签组的自动迁移及清理逻辑。
- **文件查看器**：优化超大文件加载性能并降低内存占用。

### 1.0.1 (2026-07-22)

> 修复 F3 文本预览在 App Store 沙盒环境中加载失败的问题。

### 1.0.0 (2026-07-18)

> TotalCommander 的 Python 移植版首个实现版本发布。
